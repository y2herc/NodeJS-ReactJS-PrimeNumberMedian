API URL: http://localhost:8081/primeNumber/{Any Number}

This API takes get requests and returns results as string 

Starting Application 
  After cloning repository.
  Navigate to ../touchbistro/backend/
  Run NPM Install 
  node App.js

Running test (close the above application in order to run tests)
    Navigate to ../touchbistro/backend/
    npm test